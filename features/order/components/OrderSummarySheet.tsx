import type { OrderItem } from "@/features/order/utils/buildWhatsAppMessage"

// TODO: slide-up order summary before the WhatsApp redirect.
export function OrderSummarySheet({ items }: { items: OrderItem[] }) {
  return null
}
