'use client'

// No RegisterForm — self-registration doesn't exist in this project.
export function LoginForm() {
  return (
    <form className="flex w-full max-w-sm flex-col gap-4">
      {/* TODO: React Hook Form + Zod, wired to convex/auth.ts signIn */}
    </form>
  )
}
